# GitHub Copilot quota-boundary P0 evidence — 2026-09-06

## Result

**Constrained; every real account context remains unconfirmed.** The fixture experiment keeps AI Credits separate from legacy premium requests, emits a replay-marked `local_observed` Observation only for a complete fixture shape, isolates preview SDK data as `experimental`, and preserves permission, authentication, rate-limit, endpoint-unavailable, malformed-response, and schema-change failures without emitting invented quota. The 2026-09-09 live `gh api` seam can collect official billing usage across Personal, Organization/Business, and Enterprise endpoints, but correctly leaves included allowance, remaining quota, and reset unknown because the documented Billing REST response does not contain them. See [live-contract research](copilot-antigravity-live-contract-2026-09-09.md).

Preview SDK evidence records its `public_preview` stability, the quota-read permission contract and fixture permission status, and an explicit comparison with Billing API remaining values (`matched`, `diverged`, or `not_comparable`). The Personal fixture deliberately differs by one unit to prove divergence is surfaced rather than silently reconciled. These are synthetic contract checks, not real permission or endpoint evidence.

`gh` was not discoverable on `PATH` and no GitHub token, Provider Account, organization role, or billing response was supplied. The fixture plan names and API/SDK versions are synthetic contract labels, not claims about current production versions. The live seam accepts only an explicit absolute `gh` path and relies on that CLI's existing authentication; it never accepts or prints a token.

## Reproduction

```powershell
cargo test --offline --locked --test copilot_quota -- --nocapture
cargo run --offline --locked --quiet --bin agentmeter-copilot-p0 -- --fixture tests/fixtures/copilot/contexts.json
```

After GitHub CLI is installed and authenticated, use `agentmeter-copilot-p0 collect --gh-bin <absolute-path> --context <personal|business|enterprise> --account <slug> --meter <ai-credits|premium-requests>`. The live process-double suite validates all six endpoint combinations, bounded execution, output limits, schema enforcement, and sanitized failure classification without network access.

The sanitized fixture contains a machine-readable evidence block recording Personal, Business, and Enterprise fixture plan types; the timezone-qualified test time; fixture API/SDK contract versions; observed Billing API, quota, and preview fields; limitations; and explicit `credentials_present: false` / `billing_secrets_present: false` declarations. Contract-version labels describe the replay schema and are not presented as versions observed from a live endpoint.

## Required real-account gate

For each available Personal, Business, and Enterprise context, record the plan, role, API version, required minimal scopes, endpoint, test time, billing period, units, and sanitized response fields. Exercise a permitted request plus insufficient-role, unauthenticated, rate-limited, and unavailable-endpoint results. Record unavailable contexts as evidence gaps rather than inferring access.

No Copilot context is approved for release until its real request passes. The preview SDK is not a stable substitute for Billing API evidence.
