# AgentMeter P0 capability matrix and P1 gate — 2026-09-06

## Decision summary

2026-09-09 Copilot settings follow-up: [persistent settings handoff](../handoffs/2026-09-09-desktop-copilot-settings.md) adds v1-compatible schema-v2 source persistence and explicit forget semantics without storing credentials or results. The latest rebuilt package includes refresh/save/clear commands. This is local setup work; the count remains 50/79 checked, 29 open pending real-account evidence.

2026-09-09 desktop Copilot follow-up: [desktop integration handoff](../handoffs/2026-09-09-desktop-copilot-integration.md) replaces the static Copilot card with a live operator-ready Billing usage flow backed by the same deep module as the CLI. The rebuilt package contains the command, API version and no-invented-quota guard. No real `gh` installation/account is available, so the count remains 50/79 checked, 29 open.

2026-09-09 Copilot/Antigravity contract follow-up: [official-contract research](copilot-antigravity-live-contract-2026-09-09.md) and the [handoff](../handoffs/2026-09-09-copilot-live-contract.md) add a bounded real `gh api` process seam for all documented Personal, Organization/Business, and Enterprise AI Credit/Premium Request usage endpoints. Billing usage remains distinct from remaining quota, and Antigravity stays fixture-only because no documented machine-readable contract was found. The host has neither CLI nor an available account, so no real-account tick closes; count remains 50/79 checked, 29 open.

2026-09-09 WebView/lifecycle follow-up: [startup/WebView evidence](../handoffs/2026-09-09-startup-diagnostics-webview.md) closes #05.7 and #06.2. The current host/runtime/process record is complete, and the built NSIS download-bootstrapper behavior is now documented truthfully for present, missing, unavailable-download and old-runtime cases. Current count is 50/79 checked, 29 open. Old-runtime update execution, clean-VM lifecycle and other target-environment gates remain release-blocking.

2026-09-09 follow-up: [real repeated-process evidence](../handoffs/2026-09-09-desktop-process-lifecycle.md) reclosed Windows lifecycle criterion #05.4 for the supported same-interactive-desktop scope. Five secondary launches retained one original PID and full exit left zero; inaccessible cross-desktop IPC is fail-closed. Current count is 48/79 checked, 31 open. Direct tray-menu, restart/sign-out/tray-recreation, clean-VM, authenticated Provider and physical-tablet gates remain open.

2026-09-08 follow-up: [server-side DPAPI persistence](../handoffs/2026-09-08-pair-persistence.md) is implemented and supervisor-approved for #08.2. Durable mode uses an explicit store path; ephemeral mode remains available for tests. Current count is 47/79 checked, 32 open. Browser persistence/reconnect and complete secret-hygiene evidence remain open; earlier matrix rows describe the 2026-09-07 baseline where no store existed.

2026-09-07 follow-up: the [batch handoff](../handoffs/2026-09-07-p0-batch.md) is the latest checklist and supervisor record. Claude now has an actual stdin-to-Observation wrapper path ([evidence](claude-event-path-2026-09-07.md)); Copilot permission and unit boundaries have additional regression coverage ([handoff](../handoffs/2026-09-07-copilot.md)); SSE revocation actively closes sockets ([handoff](../handoffs/2026-09-07-sse-revocation.md)). Windows single-instance acceptance was reopened after inconsistent cross-launch-environment evidence. Production release remains a no-go.

P0 validation is **incomplete and constrained; production release is a no-go until the real-world gates below are completed**. This document completes the capability-matrix publication task; it does not complete tickets 01–09. The repository now contains a real Tauri desktop lifecycle slice and a real loopback TCP/HTTP/SSE tablet-service slice in addition to fixture-driven provider and target-environment experiments. These local paths are not treated as proof of provider accounts, Windows packaging on a clean VM, ADB transport, protected persistent secrets, or end-to-end tablet viability. No capability is classified as fully `supported` for v1 on the evidence currently available.

### Classification criteria

- **Supported**: the acceptance behavior is demonstrated with reproducible evidence in the target environment and prerequisites, with no unresolved release-blocking gap.
- **Constrained**: the protocol/design seam and negative behavior are demonstrated, but a target-environment, real-account, device, packaging, or measurement gate remains. It may proceed only behind the stated limitation and setup requirement.
- **Blocked**: a required boundary is either not demonstrated even by the fixture experiment, or an unresolved safety/security decision prevents exposing it in v1. A blocked capability is not enabled by a mock pass.

## Capability matrix

| Capability | Result | Reproducible evidence | Environment / prerequisites | Quality and maturity | v1 limitation and next gate |
|---|---|---|---|---|---|
| Codex app-server quota observation | **Constrained** | [Codex evidence](codex-p0-2026-09-06.md); `cargo test --offline --locked` and `cargo run --offline --locked --bin agentmeter-p0 -- codex collect --timeout-ms 5000` | Windows build 26200; Codex Desktop app-server `0.153.4`; `codex login status` is currently not logged in | Fixture path: local-observed / experimental. Live run: unknown quota, needs-login / experimental | `account/read` worked, but rate-limit and usage reads required authentication; do not show a healthy quota card. Repeat after an explicit user login and record real methods/fields.
| Claude statusLine quota | **Constrained** | [Claude evidence](claude-p0-2026-09-06.md); `cargo test --offline --locked --test claude_statusline -- --nocapture` | Claude CLI/version/account unavailable on this host; real installation and existing statusLine config required | Fixture structured source: `local_observed` / `experimental`; log fallback: `estimated` / `experimental` | Only opt-in, reversible integration; preserve user config and label log fallback degraded. Capture real version, before/after config, events, coexistence, and cleanup.
| GitHub Copilot quota boundaries | **Constrained** | [Copilot evidence](copilot-p0-2026-09-06.md); [live contract](copilot-antigravity-live-contract-2026-09-09.md); [desktop integration](../handoffs/2026-09-09-desktop-copilot-integration.md); `cargo test --offline --locked --test copilot_quota -- --nocapture` | GitHub CLI/account/role unavailable; all six official `2026-03-10` usage endpoints and the packaged desktop card have tested local seams but no real request | Live response shape: `official` billing usage / `experimental`; remaining quota stays unknown; fixture and preview paths remain experimental | Do not conflate AI Credits with Premium Requests or usage with remaining quota. Run only with explicit absolute `gh` path and existing authentication; record real plan/role responses before release.
| Antigravity structured/fallback sources | **Constrained** | [Antigravity evidence](antigravity-p0-2026-09-06.md); [official-contract review](copilot-antigravity-live-contract-2026-09-09.md); `cargo test --offline --locked --test antigravity_quota -- --nocapture` | `agy`/Antigravity CLI and account unavailable; official docs reviewed expose interactive `/usage`, not a machine-readable quota contract | Structured fixture: `local_observed` / `experimental`; text fallback: `estimated` / `experimental` | No speculative CLI scraping. Structured source has precedence; text fallback remains prohibited until a real versioned/localized run and explicit release approval.
| Windows desktop lifecycle | **Constrained** | [Tauri production-path evidence](desktop-lifecycle-p0-2026-09-07.md); [process JSON](desktop-process-lifecycle-2026-09-09.json) | Windows 11 25H2 kernel `10.0.26200.0`; WebView2 `152.0.4191.66`; Rust/Cargo `1.95.0`; Tauri `2.11.5`; plugin `2.4.3` | Real UI/process/startup path: `local_observed` / `experimental` | Launch, close-to-hide, repeated second-instance reuse, hidden startup, startup-entry removal and abnormal-relaunch passed. Direct tray clicks, reboot/sign-out/Explorer tray recreation and clean-VM evidence remain.
| Clean install and WebView2 | **Constrained** | [Windows install evidence](windows-install-p0-2026-09-06.md); [latest package handoff](../handoffs/2026-09-09-desktop-usb-integration.md) | A statically inspected unsigned NSIS package exists; no clean VM execution | Local package/build evidence: `local_observed` / `experimental`; clean-VM measurements intentionally unknown | Installer cannot be called supported. Run the hash-pinned lifecycle runner on a disposable clean VM, including WebView2 branches, repair/reinstall/upgrade/uninstall, timing and idle resources.
| Selected-device USB loopback | **Constrained** | [TCP/HTTP production-path evidence](tablet-http-p0-2026-09-07.md); [desktop USB handoff](../handoffs/2026-09-09-desktop-usb-integration.md) | A real loopback service and bounded selected-device ADB command layer are integrated into Tauri; no ADB/device is available | Loopback/ADB process-double paths: `local_observed` / `experimental`; physical transport `not_observed` | Browser launch is never health. Operator must capture real ADB/device versions, selected-serial no-rebind mapping, authenticated reachability, reconnect/sleep/host restart and owned teardown.
| Device Pair and Tablet Session security | **Constrained** | [TCP/HTTP production-path evidence](tablet-http-p0-2026-09-07.md); [DPAPI persistence handoff](../handoffs/2026-09-08-pair-persistence.md) | Real loopback routes, OS-random credentials and current-user DPAPI Device Pair persistence exist; no physical tablet | Real HTTP/DPAPI negative and positive paths: `local_observed` / `experimental`; browser-wide secret inspection `not_observed` | Pair/session separation, protected restart persistence, route authorization, CSRF/origin, expiry, replay, rate limits, revocation and transport-generation invalidation pass locally. Browser-history/screenshots and physical-device attack/cleanup evidence remain.
| Dashboard Snapshot, SSE, async refresh | **Constrained** | [TCP/HTTP production-path evidence](tablet-http-p0-2026-09-07.md); [browser handoff](../handoffs/2026-09-09-browser-e2e.md) | Real loopback HTTP/SSE endpoints and an authenticated in-app browser run exist; no ADB-connected physical tablet | Local TCP/browser mock path: `local_observed` / `experimental`; real device measurements `not_observed` | Snapshot deduplication, revisions, heartbeat, reload recovery, async/coalesced/throttled refresh and failure isolation pass locally. Physical-tablet sleep/host-restart/session-expiry recovery, latency/resource and monitor-only evidence remain.

## Cross-experiment decisions and scope

1. A provider account is identified separately from a source and quota window. Source precedence is structured provider output first; text/log fallbacks are explicitly degraded and never silently promoted.
2. Missing, stale, unsupported, paused, authentication-failed, malformed, and schema-changed states remain distinct. Unknown values stay unknown; no collector fabricates percentages, reset times, or account identity.
3. The desktop owns the collector, server, tray, startup registration, and persistence lifecycles. The tablet is monitor-only and reaches the host through loopback plus a selected-device ADB reverse mapping.
4. Pairing is a short-lived single-use exchange; the intended Device Pair is durable; a Tablet Session is separately revocable and expiring. The current HTTP slice keeps Device Pairs only in memory, so durable protected persistence remains an open implementation gate. Every route requires authorization and state-changing routes retain origin/CSRF protection.
5. Dashboard updates are complete revisioned snapshots over SSE with heartbeat and reconnect-to-latest behavior. Refresh is asynchronous, coalesced/throttled, and must not block on a slow collector.
6. P0 fixture binaries are evidence tools, not production integrations. Their `experimental` maturity is part of the result.

## Requirements and ADR change review

This audit found **no requirements or ADR change that should be approved silently**. The corrections made to fixture evaluators align them with the existing requirements and ADRs: five pair attempts and 120-second expiry, Forget Account preserving Device Pairs, per-device ADB reverse ownership/no-rebind, account-quota deduplication without merging Source usage, canonical observation axes, and unknown measurements remaining unknown. Building the remaining package/DPAPI/ADB/tablet slices and replacing the P0 `std::net` HTTP probe with the ADR-selected adapter are follow-up implementation, not architecture changes. Any future decision to relax these boundaries, enable the Antigravity text fallback, expose LAN transport, or replace Tauri must be proposed as a separate requirements/ADR change.

## P1 go / no-go gate

**Decision: NO-GO for production P1 release; GO for bounded P1 implementation and test work.** The architecture may proceed behind feature flags and fixture/regression tests, but release publication is gated on:

- one real authenticated evidence run for each provider, with version, account/role scope, representative sanitized fields, and cleanup/config coexistence results;
- a clean supported Windows VM/Tauri run covering lifecycle, install, WebView2, restart, upgrade, uninstall, and measured resource/latency data;
- one authorized physical Android device run covering selected serial, ADB reverse, authenticated reachability, reconnect/sleep/host restart, pairing/session revocation, and owned teardown;
- real tablet rendering and monitor-only authorization evidence for snapshot, SSE ordering/heartbeat, reconnect-to-latest, asynchronous refresh, and failure isolation;
- a final security review confirming no secrets in URLs/logs/fixtures and approved Windows secret protection; and
- an explicit release decision for the Antigravity text fallback and any provider context that remains permission- or plan-dependent.

Until these gates pass, v1 scope is limited to fixture-backed development, diagnostics, and explicitly labeled experimental adapters. A missing real-account/VM/device result is a release constraint, not evidence that the capability works in production.

## Prioritized follow-up

1. Run provider account gates (Codex, Claude, Copilot, Antigravity) and convert each row from constrained only when evidence is captured.
2. Validate Windows lifecycle and clean installation/WebView2 on a clean VM, then retain reproducible logs and measurements.
3. Validate USB selection/reverse/reconnect on a physical device; use that same channel for pairing/session security tests.
4. Exercise the real tablet dashboard against the authenticated channel and measure reconnect, latency, and idle resource behavior.
5. Re-run the complete matrix and perform the final release/security review before declaring P0 validation complete or changing any capability to `supported`. Ticket 10 is resolved only as the publication of this current no-go/constrained matrix.
