# Handoff — Claude event collection — 2026-09-07

Implemented #02.3 in `src/bin/agentmeter-claude-p0.rs`: actual wrapper stdin reaches an allowlisted normalized Observation sidecar; original stdin and display stdout are preserved. Installation reports the sidecar path. Official five-hour/seven-day percentage usage and optional epoch reset are supported. Unknown source time stays null with explicit receipt time. Missing/null values yield no Observation; malformed schema fails closed.

Validation: `cargo fmt --all`; `cargo test --offline --locked --test claude_statusline` — 8 passed. New subprocess coverage exercises quota, missing/null values, invalid schema, display coexistence and sensitive-field exclusion. Details and official contract: `docs/evidence/claude-event-path-2026-09-07.md`.

Cleanup follow-up: `remove` deletes only the exact installation-owned observation sidecar after successful configuration restoration, then removes the manifest. Missing sidecar is harmless; configuration conflicts leave it untouched. The subprocess test now verifies restoration and absence of both sidecar and manifest.

Remaining: #02.7 requires real Claude version/account, sanitized authenticated event, actual configuration coexistence and cleanup evidence. No real user settings were touched. Latest-report file is a P0 single-writer sink; transactional multi-session ingestion remains outside this proof. Legacy synthetic fixture normalization remains replay-only. Changes are uncommitted; chief supervisor must review criterion #02.3 before accepting its tick.
