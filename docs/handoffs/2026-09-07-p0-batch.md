# P0 implementation batch handoff — 2026-09-07

## Current state

Formal scope: the ten issue files in `.scratch/p0-feasibility/issues`, with 79 acceptance ticks. Current implementation-backed count is 46 checked and 33 open. P0 validation and production release are not complete. Ticket 10 closes publication of the constrained capability matrix only.

| Issue | Checked | Open | Remaining gate |
|---|---:|---:|---|
| 01 Codex | 5 | 2 | Successful authenticated app-server quota/usage evidence |
| 02 Claude | 7 | 1 | Real installation/account and configuration coexistence evidence |
| 03 Copilot | 6 | 2 | Real account contexts and authoritative responses |
| 04 Antigravity | 4 | 4 | Real authenticated/versioned source, scope and fallback evidence |
| 05 Windows lifecycle | 4 | 4 | Single-instance anomaly, direct tray actions, restart/sign-out and runtime evidence |
| 06 Install/WebView2 | 1 | 7 | Actual installer and clean-VM lifecycle/resource matrix |
| 07 USB | 2 | 6 | Production ADB orchestration and physical-device evidence |
| 08 Pair/session | 5 | 3 | Durable protected Device Pair, secret hygiene and physical-tablet evidence |
| 09 Dashboard | 4 | 4 | Authenticated physical-tablet rendering/recovery and measurements |
| 10 Publication | 8 | 0 | Published constrained/no-go decision; other issues remain open |

## Completed and verified in this batch

- [Claude event path](2026-09-07-claude.md): actual wrapper stdin is normalized using the documented rate-limit fields, written to a sanitized sidecar, and forwarded unchanged to the existing display command. Removal restores configuration and deletes exact owned sidecar/manifest paths. Source timestamp stays unknown; receipt time is explicit.
- [Copilot boundaries](2026-09-07-copilot.md): explicit permission/role contracts, missing-access diagnostics, separate included allowances/overage/pool/individual fields, and fail-closed handling for unmet permission contracts or conflicting units. Missing facts stay unknown.
- [SSE revocation](2026-09-07-sse-revocation.md): active sockets close on Forget/Clear/Reset/rotation/shutdown, with expiry checks during heartbeat waits. Writes are serialized against revocation.
- [Desktop review](2026-09-07-desktop-review.md): reopened #05.4 because the inconsistent cross-launch result remains unexplained. Local plugin source identifies a missing-window branch to investigate; it does not establish the anomaly's cause.

## Validation

Root verification after implementation integration:

```text
cargo test --offline --locked
71 integration tests passed; 0 failed
cargo clippy --offline --locked --all-targets -- -D warnings
passed
cargo fmt --all -- --check
passed
git -c core.safecrlf=false diff --check
passed
```

Suite counts: Antigravity 8, Claude 8, Codex 14, Copilot 13, Dashboard fixture 2, pairing fixture 3, real TCP tablet service 10, USB fixture 6, installer fixture 3, desktop fixture 4. Earlier Tauri build/Clippy evidence is retained in `../evidence/desktop-lifecycle-p0-2026-09-07.md`; this batch did not change desktop code or repeat GUI validation.

## Chief-supervisor disposition

Final independent read-only supervisor sign-off: accepted #02.3 including sidecar cleanup, accepted #03.2/.3 after permission/unit-boundary changes, and accepted the SSE change. Retained #08.6 for its authorization-revocation scope, required #08.2/.7/.8 to remain open, and required #05.4 to reopen. The supervisor independently reran the ten TCP tablet-service tests and confirmed the 46/79 checked, 33-open count. This signs off the recorded batch, not completion of the remaining P0 gates.

## Next implementation and environment work

1. Implement durable Device Pair storage using the approved Windows protection boundary; test restart, corruption, revocation and exact cleanup. Keep #08.2/.7 open until evidence exists.
2. Investigate desktop single-instance coordination across launch environments and simultaneous launches; exercise notification-area Show/Exit and actual owned-worker cleanup.
3. Implement selected-serial ADB control with no-rebind, separate host/device ports, actionable device states and exact owned teardown; then validate on an authorized physical tablet.
4. Produce the installer and execute the clean supported Windows VM/WebView2 matrix. Local configuration alone cannot close package/VM ticks.
5. Capture real authenticated Provider evidence and physical-tablet recovery/rendering measurements. This batch's PATH check found no `adb`, `claude` or `gh`; that is a PATH observation, not proof that no installation exists elsewhere. Previous Codex authenticated quota calls returned authentication-required.

These are remaining tasks, not claimed completed work. No user credentials, live Provider settings, startup registry entries or device mappings were changed in this batch. All changes remain uncommitted; the worktree contains earlier P0 edits that must be preserved. Write a new dated handoff for each following completed batch, carrying forward unresolved items and recording only tests actually run.
