# 06: Prove clean Windows installation and WebView2 handling

**What to build:** An installable package of the minimal desktop slice that succeeds on a clean Windows VM, handles the supported WebView2 deployment path, and leaves the machine in a predictable state after install, repair, restart, upgrade, and uninstall.

**Blocked by:** 05: Prove the Windows desktop lifecycle.

**Status:** needs-info

- [ ] A clean supported Windows VM with no project toolchain can install and launch the packaged desktop slice.
- [x] Packaging uses the approved WebView2 download-bootstrapper strategy and documents what happens when the runtime is present, missing, outdated, or cannot be downloaded.
- [ ] Installer and application failures present actionable diagnostics rather than silently exiting or leaving an unusable background process.
- [ ] Restart, repair, same-version reinstall, supported upgrade, and uninstall are exercised with expected application and startup-registration behavior.
- [ ] Uninstall removes application-owned binaries and registration while preserving or removing local user data according to the approved product behavior.
- [ ] Installed size, download size, first-launch time, idle resource use, and runtime prerequisites are measured on the clean VM.
- [ ] Evidence includes the VM image details, package identity, runtime versions, test sequence, screenshots or logs, and reproducible results.
- [x] The outcome states whether Windows distribution is supported, constrained, or blocked for v1 and records any installer or WebView2 release gate.

## Comments

- 2026-09-06: Added the fixture-driven `agentmeter-windows-install-p0` probe and integration tests. It models the approved WebView2 download-bootstrapper path, present/missing runtime and download failure diagnostics, lifecycle operation results, and nullable clean-VM measurements. Real clean Windows VM/package evidence is still required before distribution can be marked supported.

- 2026-09-06 audit: Tightened the evaluator so `clean_vm: true` is never sufficient by itself: missing measurements, restart, or user-data policy now produce `needs-info`, while a missing runtime/download failure is `blocked`. Windows distribution is recorded as blocked for v1 publication pending the operator sequence in `docs/evidence/windows-install-p0-2026-09-06.md`. Criteria 1–7 remain open because this repo has no installable Tauri package or clean-VM observations; fixture values cannot close them.

- 2026-09-09 bundle-preflight follow-up: Corrected the desktop configuration from `bundle.active: false` to an active NSIS target, retained the approved silent `downloadBootstrapper` WebView2 mode, and disabled downgrades. Two offline configuration-contract tests protect these settings and package identity/version/frontend consistency. A release desktop executable builds successfully (8.70 MiB; SHA-256 recorded in the handoff), but `cargo-tauri` is not installed and no NSIS package or clean-VM run exists, so criteria 1–7 remain open. See `docs/handoffs/2026-09-09-desktop-bundle-preflight.md`.

- 2026-09-09 NSIS bundle follow-up: Installed the official `tauri-cli 2.11.4` using Tauri's documented crates.io command and successfully built `AgentMeter P0_0.1.0_x64-setup.exe`. The installer is 1,982,746 bytes with SHA-256 `E13402CC6589DEDFB2DDBA89FBD26C2E2F9B56F0ABD2AC4E4A2D471FD46B7E6A`; Authenticode reports `NotSigned`. This removes the “no installable package” implementation gap, but no clean-VM install/lifecycle/WebView2 branch has been exercised and signing remains a release gate, so criteria 1–7 remain open. See `docs/handoffs/2026-09-09-nsis-bundle.md`.

- 2026-09-09 static-inspection follow-up: Added `scripts/inspect-nsis.ps1`, which validates NSIS archive type, expected embedded executable, version, sizes, hashes, Authenticode state and presence of the NSIS download component without executing the installer. The generated setup passed: it is NSIS 3 Unicode, contains `NSISdl.dll`, and embeds a version 0.1.0 unsigned executable. The script uses a unique system-temp directory and verifies the cleanup boundary before recursive removal. This strengthens package evidence but does not replace install or clean-VM execution, so criteria 1–7 remain open. See `docs/handoffs/2026-09-09-nsis-static-inspection.md`.

- 2026-09-09 uninstall-contract follow-up: Found and fixed an application/installer mismatch: startup registration used `AgentMeterP0`, while Tauri's generated uninstaller removes `${PRODUCTNAME}` (`AgentMeter P0`). The application now uses the product name and cleans the legacy value on enable/disable. A contract test locks the names together. The rebuilt NSIS script confirms startup cleanup and Tauri's default opt-in data-deletion checkbox for `%APPDATA%\com.agentmeter.p0` and `%LOCALAPPDATA%\com.agentmeter.p0`; unchecked/default preserves both. This is build evidence, not an executed uninstall, so installer criteria remain open. See `docs/handoffs/2026-09-09-startup-uninstall-contract.md`.

- 2026-09-09 explicit-purge follow-up: Added a compiled NSIS pre-uninstall hook and explicit current-user install mode. Default and silent uninstall still preserve data; `/PURGE` explicitly sets Tauri's existing delete-data state, and the interactive checkbox remains available. The hook removes only the legacy `AgentMeterP0` startup value outside update mode and never directly deletes a path. Four contract tests pass and makensis accepted the hook in a rebuilt installer. Execution approval is still unavailable, so no lifecycle tick is closed. See `docs/handoffs/2026-09-09-nsis-explicit-purge.md`.

- 2026-09-09 lifecycle-runner follow-up: Added an opt-in, hash-pinned clean-Windows NSIS lifecycle runner with pre-mutation clean-host guards, bounded processes, hidden launch/explicit exit, startup enable/disable checks, same-version reinstall/restart, default-preserve and explicit-`/PURGE` data-policy probes, resource/timing samples, versioned success/failure JSON, and ownership-bounded cleanup. PowerShell syntax and a source-contract test are part of the complete local verifier. The runner was not executed, so criteria 1–7 remain open pending approved disposable-VM evidence. See `docs/handoffs/2026-09-09-installer-lifecycle-runner.md`.

- 2026-09-09 readiness/measurement follow-up: Replaced the runner's fixed launch delay with a side-effect-free IPC readiness probe, added measured initial/restart readiness, a default five-minute CPU/working/private-memory idle sample, and graceful-then-forced cleanup limited to runner-owned processes. Rebuilt and statically inspected that NSIS package; its SHA-256 was `0AB9A96DCA519D005158A9EB770E91971426FE3FF46649DD211573DCDB34901A` and has since been superseded. No package execution occurred, so criteria 1–7 remain open. See `docs/handoffs/2026-09-09-desktop-readiness-probe.md`.

- 2026-09-09 desktop/tablet integration follow-up: Wired the existing loopback TabletServer into the Tauri application with explicit start/stop, short-lived code generation and Clear Pairing commands. Startup creates the DPAPI store under the app's LocalAppData only after user action; ordinary status never returns a code, and full tray/IPC exit stops the listener and SSE before app exit. The UI clearly labels mock Provider data and the missing ADB mapping boundary. The rebuilt NSIS is 2,038,772 bytes with SHA-256 `3008658415A496F6FE4CB2E4C79968AD95D2CFF2D0778FA75E8C65B820A4987D`. It was not executed, so installer ticks remain open. See `docs/handoffs/2026-09-09-desktop-tablet-integration.md`.

- 2026-09-09 upgrade-runner follow-up: Extended the hash-pinned lifecycle runner with an optional old-to-new package flow. Upgrade arguments are all-or-none; both identities are checked before mutation, the new version must be greater and the hashes must differ. The runner verifies the post-upgrade product version, data, startup registration, readiness, exit and uninstall, then records upgrade timings and identity. Two contract tests and the 109-test root suite pass. No lower-version artifact or clean VM exists, so the branch is not executed and criterion 4 remains open. See `docs/handoffs/2026-09-09-installer-upgrade-runner.md`.

- 2026-09-09 desktop USB integration package follow-up: Rebuilt the package after wiring selected-device USB controls and fail-closed transport/session invalidation into Tauri. Static inspection passed for the 2,057,403-byte NSIS artifact, SHA-256 `F019358A23D9EA0B8A37E46E21382AE5D213A3C66006DC2B57972A3D409B0D3F`; its embedded v0.1.0 executable is 9,472,512 bytes with SHA-256 `A84428ED1AA125EC0DAA233556CD9D88C2987FCD48288B4AE592ADDFAB47FD76`. Both files remain unsigned and the installer was not executed, so criteria 1–7 remain open. See `docs/handoffs/2026-09-09-desktop-usb-integration.md`.

- 2026-09-09 WebView2/static-diagnostic acceptance: Audited the locked Tauri bundler 2.9.4 NSIS template against the built configuration and package. The package uses silent `downloadBootstrapper` and contains `NSISdl.dll`. A runtime found in HKLM/HKCU is skipped; a missing runtime on a fresh install downloads Microsoft's bootstrapper, runs `/silent /install`, and aborts with localized actionable text on download or nonzero install failure. No `minimumWebview2Version` is approved/configured, so an installed old runtime is currently treated as present and skipped; that limitation is now explicit and remains an AC-28 release gate rather than being misreported as automatic update. This completes criterion 2's strategy/documentation scope, not the unexecuted clean-VM branches. The desktop now also logs startup failures and shows a native WebView-independent error dialog; criterion 3 remains open until failure branches are executed in the target environment. See `docs/evidence/webview2-installer-contract-2026-09-09.md` and `docs/handoffs/2026-09-09-startup-diagnostics-webview.md`.
